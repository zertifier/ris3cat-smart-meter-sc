import {NoRoundDecimalPipe} from './no-round-decimal.pipe';

describe('NoRoundDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new NoRoundDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
